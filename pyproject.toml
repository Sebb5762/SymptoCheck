[project]
name = "symptocheck"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sebb5762", email = "sebby.popescu06@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "google-genai>=2.22.0",
    "joblib>=1.6.0",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "pillow>=12.3.0",
    "torch>=2.14.0",
    "torchvision>=0.29.0",
]

[project.scripts]
symptocheck = "symptocheck:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
